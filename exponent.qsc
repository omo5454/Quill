func Pow(n: int, x: float){
    let N: int = n;
    if(N < 0) {
        x = 1 / x;
        N = -N;
    }

    let res: float = 1.0;
    while(N > 0){
        if(N % 2 == 1) {
            res = res * x;
        }

        x = x * x;
        N = N / 2;
    }
    return res;
}


printf(Pow(3, 8.0));