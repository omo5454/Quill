# this should be ignored
let i = 0;
let x = 5; # this should also be ignored
printf(x + 15);

printf(x + 5);

# let i = i + 1;
# // this should be ignored
# // printf(x);
while x >= i{
    printf(x);
    # let i = i + 1;
    let x = x - 1;
}